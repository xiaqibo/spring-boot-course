server:
  port: 8081


team:
  leader: "xia"
  phone: "15651740820"
  age: 3
  createDate: "2019-09-01"


spring:
  servlet:
    multipart:
      max-file-size: 10MB # 单个上传文件大小限定为10M，默认值为1M
      max-request-size: 100MB # 单次上传文件大小限定为100M，默认值为10M:

  mail:
    host: smtp.qq.com # 邮件服务器地址，QQ邮箱的SMTP服务器固定写法
    username: 1650512469@qq.com # 发件人邮箱地址
    password: zmsnsfwoknkwddgf # 客户端授权码（不是登录密码，需在QQ邮箱里开通SMTP服务获取）
    protocol: smtp # 邮件发送协议，一般就是 smtp
    properties.mail.smtp.auth: true # 是否需要用户名和密码验证，QQ邮箱必须true
    properties.mail.smtp.port: 465 # SMTP端口号，常用465(SSL) 或587(TLS)
    properties.mail.display.sendmail: Spring Boot # 邮件显示的发件人别名（随意）
    properties.mail.display.sendname: Spring Boot Email # 邮件显示的发件人名称（随意）
    properties.mail.smtp.starttls.enable: true # 是否启用TLS加密，587端口一般需要
    properties.mail.smtp.starttls.required: true # 是否强制使用TLS
    properties.mail.smtp.ssl.enable: true # 是否启用SSL加密，465端口必须true
    default-encoding: utf-8 # 邮件默认字符集，防止乱码

custom:
  qrcode:
    content: "https://www.baidu.com"